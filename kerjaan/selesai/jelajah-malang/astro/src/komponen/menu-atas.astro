---
let label = ["jalan-jalan", "buku", "travel"];
---

<div class="bg-orange-300 top-0 sticky z-50">
    <div class="block px-5 py-3 sm:hidden">
        <select
            onchange="
            location.href = `#/${this.value}`
            "
            class="w-full p-2"
        >
            {
                label.map((x) => {
                    return <option>{x}</option>;
                })
            }
        </select>
    </div>
    <div class="hidden sm:block" hx-boost="true">
        {
            label.map((x) => {
                return (
                    <a
                        class="px-5 py-3 hover:bg-orange-500 hover:text-white inline-block"
                        href="/"
                    >
                        {x}
                    </a>
                );
            })
        }
    </div>
</div>
